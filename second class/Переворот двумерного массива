def readInt2D(n, m):
    a = []
    for i in range(n):
        row = input().split()
        for i in range(m):
            row[i] = float(row[i])
        a.append(row)
    return a
def printFlip2D(a):
    for i in range(m):
        for j in range(n):
            print(a[j][i], end=' ')
        print('\t')
        
n,m=map(int,input().split())
a=readInt2D(n, m)
printFlip2D(a)
